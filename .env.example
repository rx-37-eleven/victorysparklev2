# Copy to .env (gitignored) for local `npx @11ty/eleventy --serve`.
# Without these, the site still builds -- /blog/ just shows "No posts yet".
# See README-blog.md for where to find these values.

CF_ACCOUNT_ID=
CF_D1_DATABASE_ID=
CF_API_TOKEN=
